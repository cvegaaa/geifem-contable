{
  "name": "geifem-contable-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd geifem-contable/frontend && bun install --silent && bunx --bun vite --port 8080 --host 0.0.0.0",
    "build": "cd geifem-contable/frontend && bun install --silent && bunx --bun vite build"
  }
}
